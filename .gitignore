# Dependencies
node_modules/

# Secrets / auth state — never commit
.baileys_auth/
*.env
.env*

# Local databases + backups + WAL
*.duckdb
*.duckdb.*
*.wal

# Generated/runtime data — DB + regenerable caches (see lib/paths.ts)
data/caches/
data/imports/

# Generated pipeline task outputs (keep pipeline/patterns/ + run-patterns.ts source)
pipeline/task-*/
pipeline/smoke-test/
pipeline/mackenzie-scott-comprehensive/
pipeline/mackenzie-scott-research/
pipeline-grok/
pipeline-hybrid/

# Generated static site
site/

# Logs + run artifacts
logs/
batch-log.json

# Generated reports
*.html
artifacts/

# Launch artifact: verified site bytes, deliberately committed (ADR 0002 §C)
!public/
!public/**/*.html

# Local IPC/voice experiment (not part of CivicLens)
.ipc/
.grok/
scripts/ipc*.py
scripts/ipc_*.py
scripts/grok-reply.sh
scripts/voice-debate.sh
scripts/tts_render.py
*.wav
names.txt.bak

# Ad-hoc graph/probe output
graphify-out/

# Local Claude Code project config/skills — personal tooling, not part of CivicLens
.claude/
